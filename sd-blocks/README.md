# sd-blocks — Building Blocks (Referans)

**Kurs:** Ch.12, 17, 20, 32

| Sınıf | System design kullanımı |
|-------|-------------------------|
| `PrefixTrie` | Autocomplete / typeahead |
| `TokenBucketRateLimiter` | API rate limiting |
| `ConsistentHashRing` | Sharding / cache dağıtımı |
| `LruCache` | Bellek sınırlı cache |
| `InvertedIndex` | Arama motoru index |

Boot app'lerde gömülü kopyalar var; bu proje algoritmaları izole incelersin.

```bash
mvn test
```
