# sd-idgen — Snowflake ID (Referans)

**Kurs:** Ch.19

Twitter Snowflake: timestamp + machineId + sequence → 64-bit unique id.

`SnowflakeIdGenerator.java` dosyasında bit layout inline yorumlarla anlatılır.

```bash
mvn test
```
